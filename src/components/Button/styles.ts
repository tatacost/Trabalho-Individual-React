import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    backgroundColor: '#aaa',
    padding: 15,
    borderRadius: 30,
    alignItems: 'center',
  },
  buttonText: {
    fontSize: 16,
    color: '#eee',
    // backgroundColor: '#aaa',
    padding: 15,
    borderRadius: 30,
    alignItems: "center",
    marginTop: "auto"
  },
});