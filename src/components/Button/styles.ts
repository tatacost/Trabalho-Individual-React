import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    backgroundColor: '#d8d6d6',
    padding: 5,
    borderRadius: 15,
    alignItems: 'center',
  },
  buttonText: {
    fontSize: 16,
    color: '#eee',
    // backgroundColor: '#aaa',
    padding: 5,
    borderRadius: 30,
    alignItems: "center",
    marginTop: "auto"
  },
});